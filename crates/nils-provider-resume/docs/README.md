# nils-provider-resume Docs Index

> Ownership: Maintained by the `nils-provider-resume` crate maintainers. Keep this index updated when docs are added, moved, or removed.

## Specs

- None yet. The resolver surface (`resolve_resume_source`, `ResumeProvider`,
  `ResolvedResume`, `ResumeResolveError`, `normalize_resume_id`) and the scan
  primitives reused by `agent-session` are documented in the crate
  [`README.md`](../README.md).

## Runbooks

- None yet.

## Reports

- None yet. Add documents under `docs/reports/` and register them here.

## Links

- Back to crate README: [`../README.md`](../README.md)
- Consumers: `agent-session` (provider-resume import, post-launch capture, and
  transcript-path resolution), `codex-cli` (`agent resume`), and `claude-cli`
  (`agent resume`).
