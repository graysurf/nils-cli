# opencode-cli docs

`opencode-cli` owns native OpenCode prompt, advice, knowledge, and
semantic-commit prompt dispatch behavior. zsh-kit keeps only feature loading,
aliases, and compatibility wrappers.
