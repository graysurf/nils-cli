# github-app-cli docs

Crate-local docs for `github-app-cli`.

Command, option, output-mode, and exit-code documentation lives in
[`../README.md`](../README.md). This crate implements the workspace output
contract (`docs/specs/cli-output-contract-v1.md` at the repository root).

## Specs

- None yet. Add documents under `docs/specs/` and register them here.

## Runbooks

- None yet. Add documents under `docs/runbooks/` and register them here.

## Reports

- None yet. Add documents under `docs/reports/` and register them here.
