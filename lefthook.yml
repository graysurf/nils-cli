pre-commit:
  commands:
    markdown-format:
      glob: "*.md"
      run: bash scripts/ci/markdownlint-audit.sh --strict

pre-push:
  commands:
    signed-commits:
      run: bash scripts/ci/verify-signed-commits.sh
